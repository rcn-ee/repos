#!/bin/sh
#
# see: dh_installdeb(1)

set -e

package="bb-johnny-five-installer"

cleanup () {
	if [ -d /usr/local/lib/node_modules/johnny-five/ ] ; then
		rm -rf /usr/local/lib/node_modules/johnny-five/ || true
	fi

	if [ -d /var/cache/bb-johnny-five-installer/ ] ; then
		rm -rf /var/cache/bb-johnny-five-installer/ || true
	fi
}

case "$1" in
    configure)
	cleanup
    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0

