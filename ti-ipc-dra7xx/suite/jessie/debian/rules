#!/usr/bin/make -f

export DH_VERBOSE=1

confflags=--prefix=/usr \
          PLATFORM=DRA7XX \
          AF_RPMSG=4.1

%:
	dh $@ --with autoreconf

override_dh_auto_configure:
	./configure $(confflags)
