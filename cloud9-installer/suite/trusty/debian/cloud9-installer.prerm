#!/bin/sh
#
# see: dh_installdeb(1)

set -e

package="cloud9-installer"
extract_dir="/opt/cloud9/build/"

# summary of how this script can be called:
#        * <prerm> `remove'
#        * <old-prerm> `upgrade' <new-version>
#        * <new-prerm> `failed-upgrade' <old-version>
#        * <conflictor's-prerm> `remove' `in-favour' <package> <new-version>
#        * <deconfigured's-prerm> `deconfigure' `in-favour'
#          <package-being-installed> <version> `removing'
#          <conflicting-package> <version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package

remove_file () {
	if [ -f /var/cache/${package}/${wfile} ] ; then
		rm -f /var/cache/${package}/${wfile}
	fi
}

remove_dir () {
	if [ -d ${wdir} ] ; then
		rm -rf ${wdir}
	fi
}

remove_all() {
	wfile="c9v3_3.0.1-git20140211-build.tar.xz" ; remove_file
	wfile="c9v3_3.0.620-git20141219-build.tar.xz" ; remove_file
	wfile="c9v3_3.0.623-git20141222-build.tar.xz" ; remove_file
	wfile="c9v3_3.0.632-git20141222-build.tar.xz" ; remove_file
	wfile="c9v3_3.0.642-git20141227-build.tar.xz" ; remove_file
	wfile="c9v3_3.0.645-git20150102-build.tar.xz" ; remove_file
	wfile="c9v3_3.0.669-git20150111-build.tar.xz" ; remove_file

	#Always keep the current one for quick reinstalls...
	#wfile="c9v3_3.0.713-git20150117-build.tar.xz" ; remove_file

	wdir="${extract_dir}standalonebuild/" ; remove_dir
}

case "$1" in
    remove|upgrade|deconfigure)
	remove_all
    ;;

    failed-upgrade)
    ;;

    *)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0


