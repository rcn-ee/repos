#!/bin/sh

set -e

# drop the temporary mask from prerm
if [ -d /run/systemd/system ] && [ "$1" = remove ]; then
	systemctl unmask --runtime bb-wl18xx-bluetooth.service
	systemctl unmask --runtime bb-wl18xx-wlan0.service
fi

#DEBHELPER#

exit 0
