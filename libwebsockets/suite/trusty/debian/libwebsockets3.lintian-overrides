# The only GPL-licensed thing in the package is a Perl script used for
# creating the API reference.  There is no GPL-licensed code in either
# the library or the test tools.
#
libwebsockets3: possible-gpl-code-linked-with-openssl
