#!/usr/bin/make -f
# -*- makefile -*-

DEB_BUILD_OPTIONS=noautodbgsym

# Uncomment this to turn on verbose mode.
export DH_VERBOSE=1

override_dh_auto_clean:
	# Just disable for now...

%:
	dh $@ --builddirectory=uf2daemon

